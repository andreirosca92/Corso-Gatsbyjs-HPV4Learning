import styled from "styled-components"

export const ImageBox = styled.div({
  width: "80vw",
  borderRadius: "16px",
  margin: "unset auto",
  borderBlock: "1px solid var(--purple-400)",
  overflow: "hidden",
})
